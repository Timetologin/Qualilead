# QualiLead Environment Configuration

# Server
PORT=3001
NODE_ENV=development

# JWT Secret (change in production!)
JWT_SECRET=qualilead-super-secret-key-change-in-production-123456

# App URL (frontend)
APP_URL=http://localhost:5173

# Email Configuration (optional - for sending lead notifications)
# Using Gmail example - for production use a proper SMTP service
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# SMS Configuration (optional - for Twilio integration)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
